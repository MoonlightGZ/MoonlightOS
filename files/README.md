# Image Files

This directory contains files consumed by image build modules.

## Structure

- `scripts/` contains build-time scripts.
- `usr/` mirrors files that may be copied into the final image.

Keep this directory minimal. Anything placed here can become part of the operating system image.
