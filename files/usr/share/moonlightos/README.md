# MoonlightOS Image Files

This directory mirrors `/usr/share/moonlightos` for future files that should be copied into the image.

Alpha 0.1 currently uses a build-time script to create a small foundation marker instead of copying a broader file tree into the image.

